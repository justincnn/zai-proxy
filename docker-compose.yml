services:
  zai-proxy:
    image: docker.io/justincnn/zai-proxy:latest
    platform: linux/arm64
    container_name: zai-proxy
    ports:
      - "8002:8000"
    environment:
      - PORT=8000
      - LOG_LEVEL=info
    restart: always
