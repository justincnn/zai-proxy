version: '3.8'

services:
  zai-proxy:
    image: justincnn/zai-proxy:latest
    platform: linux/arm64
    ports:
      - "8001:8001"
    environment:
      - PROXY_URL=https://chat.z.ai
    restart: always